import mutations from '../../src/store/mutations';

describe('Mutation functionalities', () => {
  let state;

  beforeEach(() => {
    state = {
      currentText: '',
      nextId: 4,
      totalComponents: 3,
      components: [],
      focusComponent: {},
      imagePath: '',
    }
    return state;
  });

  test('');
});
